interface:
  display_name: "Supervised Multi-Agent"
  short_description: "Autonomous, quality-verified agent teamwork"
  default_prompt: "Use $supervised-multi-agent to complete this task with the smallest quality-preserving team, evidence for every acceptance criterion, and focused repair until verified or genuinely blocked."

policy:
  allow_implicit_invocation: true
