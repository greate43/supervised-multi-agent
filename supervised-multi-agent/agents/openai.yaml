interface:
  display_name: "Supervised Multi-Agent"
  short_description: "Quality-controlled solo or multi-agent execution"
  default_prompt: "Use $supervised-multi-agent to choose solo work or the smallest quality-preserving team, collect evidence for every acceptance criterion, and repair focused defects until verified or genuinely blocked."

policy:
  allow_implicit_invocation: true
